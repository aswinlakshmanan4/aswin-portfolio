// Deprecated — replaced by AuroraBackground.tsx in the aurora glass theme.
// The aurora drift blobs + R3F 3D scene now live at layout level so every
// page picks them up. Kept as a no-op stub for safe import compatibility.
export default function BackgroundShapes() {
  return null;
}
